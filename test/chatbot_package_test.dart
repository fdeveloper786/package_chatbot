// ignore_for_file: missing_required_param, await_only_futures

import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:chatbot_package/chatbot_package.dart';

void main() {
  test('adds one to input values', () {
    CustomButton(child: Text('Click me'), onPressed: null, style: null);
  });
}


// void main() {
//   testWidgets('Counter increments smoke test', (WidgetTester tester) async {
//     // Build our app and trigger a frame.
//     await tester.pumpWidget(CustomButton(
//       child: null,
//       onPressed: null,
//       style: null,
//     ));
//   });
// }
